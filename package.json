{
  "name": "hotel-project-csc-540-main-2",
  "version": "1.0.0",
  "description": "Hotel App ## Local Setup 1. Copy `config.example.php` → `config.php` (do NOT commit). 2. Create MySQL DB `hotelapp` in phpMyAdmin. 3. Run schema + seed SQL (see `/sql/` folder). 4. Start XAMPP (Apache + MySQL). 5. Visit `http://localhost/Hotel-Project-CSC-540/`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
